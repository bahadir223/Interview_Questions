package day18constructorsstatickeyword;

public class MethodOverloading {
    /*
        1) Bir method, aynı isim ve farklı parametrelerle oluşturulursa buna "Method Overloading" denir.

        2) "Method Overloading" , Java'da "İyi organizasyon" için önemlidir.

        3) "Method Overloading" yaparken, parametre 3 şekilde değiştirilebilir:
            i)Parametrelerin sayıları değiştirilebilir.
            ii)Parametrelerin data type'leri değiştirilebilir.
            ii)Parametrelerin data type'leri farklı ise yerleri değiştirilebilir.

        4)"Method Overloading" bir class'in içinde oluşur bu yüzden "private" methodlar da
           overload edilebilir."Method Overloading" bütün access modifier'larda yapılabilir.

        5) "static" method'lar "overload" edilebilirler.
     */
}
